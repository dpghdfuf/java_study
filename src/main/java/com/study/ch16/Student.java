package com.study.ch16;

import lombok.AllArgsConstructor;
import lombok.Data;
/*
@AllArgsConstructor
@Data

public class Student {
    private String name;
    private int age;

    public int getAge() {
        return age;
    }

    public void setAge(int age) {
        this.age = age;
    }

    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;

    Student[] studentArray = new Student[4];
    for(int i = 0; i < studentArray.length; i++) {
        Student student;
        student = new Student("김준일", 31);
        studentArray[i] = new Student("김준일", 31);

    }

    int i = 0;    // 향상된 for문으로 하지 말고 위에처럼 하기
    for(Student student : studentArray) { //
        studentArray[i] = new Student


    }

    // Students.get[0] = new Student["김준일", 31]; 이렇게 넣을 수 없다. 마치 10 = 10과 같다.


    //arrayList는
    //students.set(0, new Student("김준일", 31)); // -> 이렇게 사용하기 // 이것이 set함수이다.
    // 주소에다가 값 집어넣지 말기!!!! 조심하기!!!
}
*/