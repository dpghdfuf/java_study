package com.study.ch16;

import java.util.ArrayList;
/*
public class StudentListMain {
    public static void main(String[] args) {


        ArrayList<Student> students = new ArrayList<>();
        students.add(new Student("김준일", 31));
        students.add(new Student("김준2", 32));
        students.add(new Student("김준3", 33));
        students.add(new Student("김준4", 34));

        System.out.println(students);

        //Student s = new Student("aaa",10);
        //s.setAge(s.getAge() + 1);

        //students.get(0).setAge(students.get(0) + 1) // 학생 9번의 주소. 나이를 바꾸겠다. 원래 학생의 나이  +1
       for (int i = 0; i < students.size(); i++) {
            // 주소들이 배열에 들어있다
            students.get(i).setAge(students.get(i).getAge() + 1 );   //중요 객체가 어떻게 빠져나오고. 데이터가 어떻게 흘러가는지 담겨있는지 무조건 알아야한다.
           // 학생들의 묶으면서 get 0번의 주소를 타고 들어가면 set이 있고 첫번째 나이를 set해준다 첫번째 학생의 원래 나이의 학생의 나이를 가져와서 1을
            // 더한다
        }

        for(int i = 0; i< students.size(); i++) {
            Student student = students.get(i);
            student.setAge(student.getAge() + 1);
        }
        // 위의 for문을 줄이면(향상된 for문이라 한다)
        for(Student student : students) {  // 리스트 뿐만 아니라 배열에서도 뒤에서 배우는 OO에서도 가능 // 인덱스 없는 것 // students는 현재 배열이다?리스트이다?
            // students에서 처음부터 꺼내서 student에 변수 처음부터 끝까지 넣는다.
            student.setAge(student.getAge() +1 );
        }

        System.out.println(students);

           :
    }
}
*/
